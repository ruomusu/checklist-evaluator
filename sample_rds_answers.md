# RDS 测试答案（混合正确和错误内容）

## Q1: EBS优化实例
EBS优化实例提供实例到EBS的专用吞吐量，防止网络拥塞，提高I/O性能。优势是稳定的IOPS和可预测的吞吐量。

支持的RDS实例类型包括db.m5及以上、db.r5及以上、db.t3及以上。可以在创建DB实例时查看实例类型是否支持EBS优化。

查看最大和基准性能可以参考AWS官方文档的RDS数据库实例类规格表。

不过我觉得db.t2也支持EBS优化，只是需要手动开启。

## Q2: RDS存储条纹化
RDS会自动将数据分布在多个EBS卷上进行条纹化，实现并行读写提高性能。这个过程对用户透明，不需要手动配置。

通用SSD（gp2）提供3 IOPS/GB，最高16000 IOPS。预配置IOPS SSD（io1）可以独立配置IOPS和存储大小。

条纹化机制对所有数据库引擎（MySQL、PostgreSQL、Oracle、SQL Server）都是相同的。

我觉得RDS不会自动条纹化，需要用户在创建时手动配置RAID。

## Q3: 修改数据库参数
使用默认参数组时不能直接修改参数。步骤：
1. 创建自定义参数组（选择相同引擎版本）
2. 在新参数组中修改所需参数
3. 修改DB实例关联到新参数组
4. 根据参数类型决定是否需要重启

有些参数立即生效，有些需要重启。常见修改：max_connections、slow_query_log、innodb_buffer_pool_size。

最佳实践：在非生产环境测试、注意参数相互影响、准备回滚方案。

## Q4: RDS主用户权限
主用户权限范围：创建数据库、创建用户和角色、管理权限、执行大多数SQL操作。

限制：无法修改某些系统参数、无法停止数据库服务、无法直接访问底层操作系统。

重置密码：通过控制台或CLI（aws rds modify-db-instance --master-user-password）修改。

最佳实践：不在应用中暴露主用户，创建受限用户做日常操作，定期更改密码。

不过我觉得主用户有root权限，可以做任何操作包括修改系统文件。

## Q5: Multi-AZ故障转移原因
自动故障转移场景：
- 数据库实例故障（进程崩溃、内存耗尽）
- 实例硬件故障（CPU、内存、磁盘故障）
- 网络问题（连接中断）
- 存储故障（EBS卷故障、I/O超时）
- 操作系统故障（内核崩溃）
- 主动维护操作

故障转移过程：检测故障（约1分钟）→ 备用实例激活 → DNS更新（约2分钟）→ 应用重连。

影响包括短暂连接中断和需要应用重连逻辑。

我记得故障转移过程完全无感知，应用不会有任何中断。

## Q6: 查看数据库事件
方法：
1. 控制台：RDS控制台 → 选择实例 → 事件标签
2. CLI：aws rds describe-events --source-identifier mydb --source-type db-instance
3. 事件订阅：创建SNS主题接收通知

事件类型包括实例启动/停止、参数修改、备份完成、故障转移、维护活动。事件保留14天，之后自动删除。

最佳实践：设置事件订阅及时获取通知，定期分析趋势。

## Q7: 存储自动扩展触发条件
触发条件：
- 可用存储低于10%（或接近容量）
- 低可用存储状态持续至少5分钟
- 距上次扩展至少6小时（冷却期）

扩展大小：通常增加5GB或当前存储的10%（取较大值），最少增加1GB。

限制：不能缩小存储、达到最大存储后停止扩展、需设置最大存储上限。各引擎最大支持65536GB。

不过我觉得自动扩展没有冷却期，只要空间不够就会立即扩展。

## Q8: 修改RDS实例的影响判断
无停机修改：部分参数修改、标签更改、安全组修改、备份保留期修改。

需要停机的修改：引擎版本升级、实例类型更改、存储类型更改、Multi-AZ启用/禁用。

判断方法：控制台修改对话框会显示"应用方式"选项（立即或需要重启）、查看官方文档参数标记。

风险最小化：在非生产环境测试、选择低峰期应用、准备回滚计划、配置应用重连。

## Q9: 未加密实例迁移到加密实例
RDS不支持原地加密，需要迁移。步骤：
1. 对未加密实例创建快照
2. 复制快照时选择KMS密钥进行加密
3. 从加密快照恢复新数据库实例
4. 验证数据完整性后切换应用连接
5. 删除旧实例

也可以使用DMS（Database Migration Service）进行在线迁移，最小停机。

我觉得直接修改实例属性就可以开启加密，不需要这么复杂的迁移过程。

## Q10: CloudWatch监控和增强监控
标准CloudWatch指标：CPUUtilization、DatabaseConnections、FreeStorageSpace、ReadIOPS、WriteIOPS、ReadLatency、WriteLatency、NetworkThroughput、BinLogDiskUsage等。

增强监控（Enhanced Monitoring）：提供操作系统级别的细粒度指标，包括进程信息、系统负载、文件系统活动。需要创建IAM角色（AmazonRDSEnhancedMonitoringRole），粒度可设1-60秒。

Performance Insights提供数据库工作负载可视化和等待事件分析。

关键告警建议：CPU>80%、存储>85%、连接数接近最大值。

## Q11: 读副本延迟原因
延迟原因分析：
- 网络延迟：跨AZ或跨区域复制
- 主实例高写入负载：大量DML操作导致副本跟不上
- 大型事务：长事务执行导致日志应用缓慢
- 副本计算能力不足：实例类型太小
- 副本I/O性能不足：磁盘IOPS限制
- 复杂查询在副本执行：阻塞复制线程

监控方式：CloudWatch的ReplicationLag指标、SHOW SLAVE STATUS的Seconds_Behind_Master字段。

减少延迟方法：升级副本实例、优化主实例写入、启用多线程复制（MySQL 5.7+）。

不过我觉得读副本延迟是正常现象，一般有5-10分钟延迟是可接受的。
