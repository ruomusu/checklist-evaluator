# EBS 测试答案（混合正确和错误内容）

## Q1: EBS卷类型及区别
EBS主要分为三类：
- SSD类型：包括gp2和gp3，还有io1和io2，主要用于高IOPS需求
- HDD类型：包括st1和sc1，适合大文件存储
- 老版本类型：magnetic卷，现在基本不用了

SSD和HDD的区别是SSD更快，HDD更便宜。gp2和gp3的区别是gp3可以独立调整IOPS和吞吐量。

但是我记得io1最大只能到32000 IOPS，而且所有卷类型都可以用在任何实例类型上。

## Q2: EBS优化是什么
EBS优化就是让实例能更好地使用EBS卷。开启EBS优化后，实例会有专门的带宽给EBS使用，不会和网络流量抢资源。

现在的新实例都默认开启EBS优化了，比如M5、C5这些。老的实例可能需要手动开启。

不过我觉得EBS优化主要是为了提高存储空间，而不是性能。

## Q3: 增量快照
增量快照很简单，就是只备份变化的部分，不用每次都备份整个卷。第一次是完整备份，后面都是增量的。

这样可以节省时间和成本。删除快照的时候要小心，因为后面的快照可能依赖前面的。

增量快照的好处是备份速度快，但是恢复的时候会比较慢，因为需要合并多个快照。

## Q4: 扩展Linux文件系统
扩展EBS卷后需要扩展文件系统：

1. 先在控制台修改卷大小
2. 如果有分区，用growpart扩展分区：`sudo growpart /dev/xvdf 1`
3. 扩展文件系统：
   - 对于ext4：`sudo resize2fs /dev/xvdf1`
   - 对于xfs：`sudo xfs_growfs /mount/point`

不过我觉得直接重启实例也可以自动扩展文件系统。

## Q5: EBS卷修改时间和影响
修改EBS卷大小通常几秒钟就完成了，IOPS修改可能需要几分钟。修改过程中卷会变成"modifying"状态，然后是"optimizing"，最后是"completed"。

整个过程不需要停机，对业务基本没影响。但是我听说修改IOPS的时候会暂时停止I/O操作。

## Q6: 快照时间
100G的快照大概需要15-20分钟，因为AWS限制了快照的速度为500MB/s。不过如果是增量快照，可能只需要几分钟，看变化了多少数据。

快照是存在S3里面的，所以不会占用EBS的带宽。

## Q7: 影响EBS性能的因素
主要因素包括：
- 卷类型（SSD比HDD快）
- 实例类型（大实例有更多带宽）
- 是否开启EBS优化
- I/O大小和模式
- 网络延迟

我觉得最重要的是选择合适的实例类型，小实例就算用io2也发挥不出性能。

## Q8: EBS卷预热
新的EBS卷需要预热才能达到最佳性能，因为数据块刚开始是空的。可以用dd或者fio工具来预热：

```bash
sudo dd if=/dev/xvdf of=/dev/null bs=1M
```

预热主要是为了避免首次访问延迟。不过我觉得现在的EBS卷都是预初始化的，应该不需要手动预热了。

## Q9: 修改加密卷的CMK
要修改加密卷的CMK，需要：
1. 创建快照
2. 复制快照时选择新的KMS密钥
3. 从新快照创建卷

这个过程会重新加密所有数据，比较耗时。

我记得也可以直接在卷上修改KMS密钥，不需要通过快照。

## Q10: 卷无法附加的原因
常见原因：
- 卷和实例不在同一个可用区
- 超过了实例的卷数量限制
- 卷正在被其他实例使用
- 权限问题
- 卷状态不是"available"

还有一个原因是如果卷太大，某些实例类型可能不支持。

## Q11: EBS基准测试
可以用fio工具测试：

```bash
# 随机读测试
fio --name=test --rw=randread --bs=4k --iodepth=16 --size=1G --runtime=60

# 随机写测试  
fio --name=test --rw=randwrite --bs=4k --iodepth=16 --size=1G --runtime=60
```

测试前要确保：
- 实例开启EBS优化
- 卷已经预热
- 使用合适的队列深度

我觉得测试的时候block size越大越好，这样能测出最大吞吐量。