# ELB 测试答案（混合正确和错误内容）

## Q1: CLB/ALB/NLB区别
AWS有三种负载均衡器：
- ALB（应用负载均衡器）：工作在第7层（应用层），支持基于内容的路由，支持HTTP、HTTPS、gRPC协议
- NLB（网络负载均衡器）：工作在第4层（传输层），支持TCP、UDP、TLS协议
- CLB（经典负载均衡器）：混合模式，支持HTTP、HTTPS、TCP、SSL

ALB适合Web应用，NLB适合高性能低延迟场景。CLB是老一代的，现在不推荐使用了。

不过我觉得NLB也能做基于内容的路由，只是性能比ALB更好。

## Q2: ELB负载均衡算法
ALB使用循环轮询（Round Robin）和最小未完成请求算法。NLB使用流哈希算法（Flow Hash）。CLB使用循环轮询。

ELB接收请求后会执行健康检查，选择健康的目标实例转发请求，然后返回响应。

我记得ALB默认使用加权随机算法，不是循环轮询。NLB也支持最小连接数算法。

## Q3: Internet-facing vs Internal ELB
Internet-facing ELB：有公有IP地址，可以从互联网访问，适合Web服务器和API网关。

Internal ELB：只有私有IP地址，只能从VPC内部访问，适合内部应用层和后端服务。

两者的主要区别就是是否有公网IP。Internal ELB完全无法从外部访问。

## Q4: 健康检查和Fail Open
ELB定期检查目标实例的健康状态，只把流量路由到健康目标。配置参数包括：协议和端口、检查间隔（默认30秒）、超时时间（默认5秒）、健康阈值（默认3次）、不健康阈值（默认3次）。

Fail Open机制：当所有目标都不健康时，如果启用了Fail Open，负载均衡器会继续路由流量到所有目标；如果禁用则拒绝所有流量。

不过我觉得Fail Open是默认关闭的，需要手动开启才能生效。

## Q5: ELB扩展机制
ELB会自动扩展来处理流量增长。ALB通过监控流量和连接数来增加扩展单位。NLB支持每秒数百万连接，通过预连接扩展实现。

扩展通常需要几分钟时间，不会立即应用。所以突发流量可能会导致短暂的性能问题。

我觉得ELB扩展是瞬时的，不会有延迟，任何流量突增都能立即处理。

## Q6: ELB稳定IP
目的：确保DNS名称对应的IP不变，满足第三方API对接、防火墙规则、合规要求。

实现方式：NLB支持分配弹性IP（EIP），每个AZ一个EIP，IP永久不变。ALB不直接支持静态IP，需要通过Global Accelerator或NLB前置来实现。

我记得ALB也可以直接分配弹性IP，跟NLB一样。

## Q7: ELB预热
预热是向AWS提前告知预期的流量，让AWS提前为负载均衡器分配资源。

使用场景：营销活动、定时直播事件、压力测试前、流量模式预测。申请预热需要提供开始时间、持续时间、每秒请求数等信息。

通过提交AWS support ticket来申请预热。预热一般提前48小时申请。

## Q8: ELB访问日志
ELB访问日志用途：故障排查、安全分析、性能分析、合规审计。日志存储在S3中。

ALB访问日志记录HTTP请求详细信息，包括协议、状态码、客户端IP、请求处理时间等。NLB记录TCP/UDP连接信息，字段较少，主要是协议、IP、端口。

我觉得ALB和NLB的访问日志格式完全一样，没有区别。都是默认开启的。

## Q9: ELB 5xx错误原因
- 502 Bad Gateway：目标实例离线、故障、健康检查失败
- 503 Service Unavailable：所有目标不健康、后端过载
- 504 Gateway Timeout：目标响应超时（超过idle timeout设置）

排查方法：查看CloudWatch指标、分析访问日志、检查目标实例健康状态。

504错误最常见的原因是后端应用处理时间太长，需要优化应用或增加超时设置。

## Q10: 获取客户端IP
ELB转发请求后，后端看到的源IP是ELB内部IP而非真实客户端IP。

解决方法：使用X-Forwarded-For头获取原始客户端IP。还有X-Forwarded-Proto（原始协议）和X-Forwarded-Port（原始端口）。

应用代码需要从这些头中提取真实客户端IP。对于NLB，如果使用TCP模式可以用Proxy Protocol来传递客户端IP。

不过我觉得NLB默认就会保留客户端IP，不需要额外配置。
