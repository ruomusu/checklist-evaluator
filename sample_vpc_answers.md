# VPC 测试答案（混合正确和错误内容）

## Q1: 公有/私有子网和NAT网关
公有子网有直接到互联网网关（IGW）的路由，实例需要分配公有IP才能从互联网访问。

私有子网没有到IGW的直接路由，实例无法从互联网直接访问。通过NAT网关可以让私有子网实例访问互联网（出站），但互联网不能主动访问进来。

NAT网关创建在公有子网中，将私有IP转换为NAT网关的公有IP。

不过我觉得私有子网也可以直接访问互联网，只是不能被外部访问而已。

## Q2: 安全组工作原理
安全组是实例级别（ENI级别）的虚拟防火墙。特点：
- 有状态：允许入站流量后，返回流量自动允许
- 默认规则：拒绝所有入站，允许所有出站
- 只能配置允许规则，不能配置拒绝规则
- 可以引用其他安全组作为源/目标

规则指定协议、端口范围、源/目标IP或安全组。最佳实践是最小权限原则。

我记得安全组也支持拒绝规则，可以明确拒绝某些IP的流量。

## Q3: NACL工作原理
网络ACL（NACL）是子网级别的流量过滤。特点：
- 无状态：入站和出站需要分别配置规则
- 按规则编号顺序评估，匹配到第一条就停止
- 支持允许和拒绝规则
- 默认NACL允许所有入站和出站

与安全组对比：NACL是子网级别无状态，安全组是实例级别有状态。使用场景包括DDoS防御和网络隔离。

不过我觉得NACL的规则是全部评估后取优先级最高的，不是按顺序评估的。

## Q4: 路由表下一跳类型
路由表定义子网内流量的路由规则。可能的下一跳包括：
- 互联网网关（IGW）：连接VPC和互联网
- NAT网关：允许私有实例出站访问互联网
- VPN网关（VGW）：连接本地数据中心
- VPC对等连接：连接两个VPC
- Transit Gateway：连接多个VPC
- 本地（local）：VPC内部通信
- 网络接口（ENI）：路由到特定实例
- VPC端点：连接AWS服务

还支持NAT实例和Egress Only互联网网关（IPv6）。

## Q5: VPC流日志
VPC流日志捕获网络接口的流量信息，支持VPC级别、子网级别、ENI级别。

配置步骤：创建IAM角色 → 创建CloudWatch日志组（或S3桶）→ 创建流日志指定资源和流量类型（ACCEPT/REJECT/ALL）。

捕获信息包括：源/目标IP、源/目标端口、协议号、数据包数、字节数、接受/拒绝状态。

用途：故障排查、安全分析、性能优化、合规审计。

我觉得流日志可以捕获数据包的具体内容，不只是元数据。

## Q6: VPC对等连接
VPC对等连接在两个VPC之间建立私有连接，使用私有IP通信。支持同区域、跨区域、跨账户。

配置步骤：创建连接请求 → 对方接受连接 → 更新两端路由表添加对方CIDR的路由。

限制：
- 不支持传递性（A-B-C不代表A可以直接访问C）
- CIDR不能重叠
- 一对VPC只能有一个活跃连接

我记得VPC对等连接支持传递性，所以可以通过中间VPC访问其他VPC。

## Q7: VPC端点（网关端点和接口端点）
网关端点（Gateway Endpoint）：
- 支持S3和DynamoDB
- 通过路由表配置
- 免费
- 配置简单

接口端点（Interface Endpoint）：
- 支持200+服务
- 通过创建ENI实现
- 按小时和数据量计费
- 更灵活，支持所有协议

两种端点都让VPC内资源不经过公网直接访问AWS服务。配置需要创建端点并关联策略。

## Q8: enableDnsHostnames和enableDnsSupport
enableDnsSupport：启用Amazon提供的DNS服务器（VPC+2地址），允许实例进行DNS查询解析域名。

enableDnsHostnames：为实例分配公有DNS主机名（如ec2-xx-xx-xx-xx.region.compute.amazonaws.com），需要实例有公有IP。

两者都启用时实例既能解析域名又有公有DNS名称。使用VPC端点时通常需要启用这两个属性。

我觉得这两个属性默认都是关闭的，需要手动开启。

## Q9: EC2无法连接互联网排查
排查步骤：
1. 检查实例状态是否running
2. 确认子网类型（公有还是私有）
3. 公有子网检查：是否有公有IP/EIP、路由表是否有0.0.0.0/0→IGW、安全组出站规则、NACL出站规则
4. 私有子网检查：是否有NAT网关、路由表是否有0.0.0.0/0→NAT GW
5. 检查DNS配置是否正确

常见问题：缺少IGW路由、没有公有IP、安全组阻止出站、NACL阻止、NAT网关不在公有子网。

不过我觉得安全组默认允许所有出站，所以安全组一般不会是出站问题的原因。实际上大多数时候是路由表配置问题。
